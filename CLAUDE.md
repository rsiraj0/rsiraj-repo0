# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently a scaffold — it contains only a `README.md` with the project title (`rsiraj-repo0`) and has no source code, build configuration, dependencies, or tests yet.

When code is added, update this file with:
- Build / lint / test commands (including how to run a single test)
- High-level architecture that spans multiple files
- Any project-specific conventions that aren't obvious from reading the code

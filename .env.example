# Optional: enables real AI-generated components. If unset, UIGen returns
# a static component instead of calling the Anthropic API.
ANTHROPIC_API_KEY=

# JWT secret used to sign session cookies. Change in production.
AUTH_SECRET=change-me-in-production

# SQLite database URL used by Prisma.
DATABASE_URL="file:./dev.db"
